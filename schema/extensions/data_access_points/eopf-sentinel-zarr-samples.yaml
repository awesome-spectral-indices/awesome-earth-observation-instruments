$schema: https://json-schema.org/draft/2020-12/schema
$id: https://raw.githubusercontent.com/awesome-spectral-indices/awesome-earth-observation-instruments/main/schema/extensions/data_access_points/eopf-sentinel-zarr-samples.yaml
title: EOPF Sentinel Zarr Samples Extension
type: object

required:
  - stac_endpoint
  - primary

properties:

  stac_endpoint:
    type: string
    description: EOPF Sentinel Zarr Samples STAC API endpoint
    const: "https://stac.core.eopf.eodc.eu/"

  primary:
    type: object
    description: Primary EOPF Sentinel Zarr Samples dataset metadata for this instrument
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of the primary data
      collection:
        type: string
        description: EOPF Sentinel Zarr Samples STAC collection name

  raw:
    type: object
    description: Optional EOPF Sentinel Zarr Samples metadata for raw (unprocessed) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of raw data
      collection:
        type: string
        description: EOPF Sentinel Zarr Samples STAC collection name

  boa:
    type: object
    description: Optional EOPF Sentinel Zarr Samples metadata for bottom-of-atmosphere (BOA) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of BOA data
      collection:
        type: string
        description: EOPF Sentinel Zarr Samples STAC collection name

  toa:
    type: object
    description: Optional EOPF Sentinel Zarr Samples metadata for top-of-atmosphere (TOA) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of TOA data
      collection:
        type: string
        description: EOPF Sentinel Zarr Samples STAC collection name

  lst:
    type: object
    description: Optional EOPF Sentinel Zarr Samples metadata for land surface temperature (LST) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of LST data
      collection:
        type: string
        description: EOPF Sentinel Zarr Samples STAC collection name

  wst:
    type: object
    description: Optional EOPF Sentinel Zarr Samples metadata for water surface temperature (WST) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of WST data
      collection:
        type: string
        description: EOPF Sentinel Zarr Samples STAC collection name

  grd:
    type: object
    description: Optional EOPF Sentinel Zarr Samples metadata for ground range detected (GRD) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of GRD data
      collection:
        type: string
        description: EOPF Sentinel Zarr Samples STAC collection name

  rtc:
    type: object
    description: Optional EOPF Sentinel Zarr Samples metadata for radiometrically terrain corrected (RTC) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of RTC data
      collection:
        type: string
        description: EOPF Sentinel Zarr Samples STAC collection name

additionalProperties: false
