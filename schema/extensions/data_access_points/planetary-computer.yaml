$schema: https://json-schema.org/draft/2020-12/schema
$id: https://raw.githubusercontent.com/awesome-spectral-indices/awesome-earth-observation-instruments/main/schema/extensions/data_access_points/planetary-computer.yaml
title: Planetary Computer Extension
type: object

required:
  - stac_endpoint
  - primary

properties:

  stac_endpoint:
    type: string
    description: Planetary Computer STAC API endpoint
    const: "https://planetarycomputer.microsoft.com/api/stac/v1"

  primary:
    type: object
    description: Primary Planetary Computer dataset metadata for this instrument
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of the primary data
      collection:
        type: string
        description: Planetary Computer STAC collection name

  raw:
    type: object
    description: Optional Planetary Computer metadata for raw (unprocessed) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of raw data
      collection:
        type: string
        description: Planetary Computer STAC collection name

  boa:
    type: object
    description: Optional Planetary Computer metadata for bottom-of-atmosphere (BOA) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of BOA data
      collection:
        type: string
        description: Planetary Computer STAC collection name

  toa:
    type: object
    description: Optional Planetary Computer metadata for top-of-atmosphere (TOA) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of TOA data
      collection:
        type: string
        description: Planetary Computer STAC collection name

  lst:
    type: object
    description: Optional Planetary Computer metadata for land surface temperature (LST) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of LST data
      collection:
        type: string
        description: Planetary Computer STAC collection name

  wst:
    type: object
    description: Optional Planetary Computer metadata for water surface temperature (WST) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of WST data
      collection:
        type: string
        description: Planetary Computer STAC collection name

  grd:
    type: object
    description: Optional Planetary Computer metadata for ground range detected (GRD) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of GRD data
      collection:
        type: string
        description: Planetary Computer STAC collection name

  rtc:
    type: object
    description: Optional Planetary Computer metadata for radiometrically terrain corrected (RTC) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of RTC data
      collection:
        type: string
        description: Planetary Computer STAC collection name

  slc:
    type: object
    description: Optional Planetary Computer metadata for single look complex (SLC) products
    additionalProperties: false
    required:
      - docs
      - collection
    properties:
      docs:
        type: string
        description: Link to the documentation of SLC data
      collection:
        type: string
        description: Planetary Computer STAC collection name

additionalProperties: false
